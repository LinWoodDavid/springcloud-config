server:
  port: 6090
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:6098/

management:
  endpoints:
    web:
      exposure:
        #动态刷新
        include: refresh